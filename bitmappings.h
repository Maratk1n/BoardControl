#ifndef BITMAPPINGS_H
#define BITMAPPINGS_H

#include <QWidget>

class BitMappings : public QWidget
{
    Q_OBJECT
public:
    explicit BitMappings(QWidget *parent = nullptr);

signals:

public slots:
};

#endif // BITMAPPINGS_H